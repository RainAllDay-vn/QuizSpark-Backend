spring:
  datasource:
    url: jdbc:postgresql://db.mkxmdxihrygvartegmhd.supabase.co:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: ${DB_SUPABASE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: quiz_spark

  h2:
    console:
      enabled: false